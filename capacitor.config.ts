import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.callrabbitsfoot.ownerhub',
  appName: "Rabbit's Foot Owner Hub",
  webDir: 'dist'
};

export default config;
